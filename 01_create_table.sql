CREATE OR REPLACE TABLE sales (
    sale_id INTEGER,
    sale_date DATE,
    customer_id INTEGER,
    product STRING,
    category STRING,
    quantity INTEGER,
    unit_price INTEGER,
    region STRING
);
